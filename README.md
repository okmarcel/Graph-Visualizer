# Graph Visualizer

# Building
Use gradle to build and run the project automatically with:
```
./gradlew run
```

or just to build:
```
./gradlew build
```

To clear cache after build run:
```
./gradlew clean
```
